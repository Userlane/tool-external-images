# infra-analytics-core
# PLT-3229 hop 1: both KafkaConnect clusters use spec.build, so the operator needs a
# matching kaniko executor to rebuild their images.
# FROM quay.io/strimzi/kaniko-executor:1.1.0
FROM quay.io/strimzi/kaniko-executor:0.37.0

# infra-analytics-core
# PLT-3229 hop 1: pinned back to 0.37.0 so the tag exists before the operator bump.
# Renovate will move this forward again; tags are immutable, so 0.37.0 survives.
# FROM quay.io/strimzi/operator:1.1.0
FROM quay.io/strimzi/operator:0.37.0
